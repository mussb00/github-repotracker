import React from 'react'

const RepoCard = ({userName, name}) => {

    return (
        <div>
            <p>{userName}</p>
            <p>{name}</p>
        </div>
    )

}


export default RepoCard
